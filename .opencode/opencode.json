{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "pwsh": {
      "template": "Run the following PowerShell command on Windows from WSL: {{input}}",
      "description": "Run a PowerShell command on Windows from WSL"
    }
  }
}
